type BlockchainComAddress = {
    hash160: string,
    address: string,
    n_tx: number,
    total_received: number,
    total_sent: number,
    final_balance: number,
}

export {
    BlockchainComAddress
}